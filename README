chitato
=======

Game of "Tic-tac-toe" written in Common Lisp

Dependencies
------------

- Common Lisp compiler (sbcl)
- Common Lisp package manager (quicklisp)
- ncurses (cl-charms)

Compiling and Running
---------------------

Building chitato:

    $ ./build.sh
    $ ./build/chitato

Run chitato without compiling:
    
    $ ./run.sh

Or, you can set up a live REPL session and call
`(chitato:main)` from "src/chitato.lisp".
